run: "docker compose up --build"
seed: "python -m app.seed"
test: "pytest"
base_url: "http://localhost:8000"
endpoints:
  health: "GET /health"
  config: "GET /widgets/{id}/config"
  submit: "POST /submissions"
  dashboard: "GET /api/dashboard/stats"